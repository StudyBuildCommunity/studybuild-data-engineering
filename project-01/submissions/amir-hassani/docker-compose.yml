services:
  etl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fmcg-inventory-etl
    working_dir: /app
    volumes:
      - .:/app
    command: python src/data_generation/generate_raw_data.py